<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hospital Dashboard</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="dashboard-container">
        <h2>Hospital Dashboard</h2>
        <div class="hospital-info">
            <p><strong>Hospital ID:</strong> <%= data[0].hospitalid %></p>
            <p><strong>Name:</strong> <%= data[0].name %></p>
            <p><strong>Address:</strong> <%= data[0].address %></p>
            <p><strong>State:</strong> <%= data[0].state %></p>
            <p><strong>City:</strong> <%= data[0].city %></p>
            <p><strong>Pincode:</strong> <%= data[0].pincode %></p>
            <p><strong>Customer Care no.:</strong> <%= data[0].customercre %></p>
        </div>
        <table>
            <thead>
                <tr>
                    <th>Patient ID</th>
                    <th>Disease name</th>
                    <th>Treated from</th>
                    <th>Treated to</th>
                    <th>Current status</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach(record => { %>
                    <tr>
                        <td><%= record.patientid %></td>
                        <td><%= record.diseasename %></td>
                        <td><%= record.treatedfrom %></td>
                        <td><%= record.treatedto %></td>
                        <td><%= record.currentstatus %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
        <a href="/logout">Logout</a>
    </div>
</body>
</html>
